{
  "name": "jenkins-exam-html",
  "version": "1.0.0",
  "description": "Simple HTML for Jenkins Exam",
  "scripts": {
    "test": "echo 'Testing... OK' && exit 0",
    "build": "if not exist dist mkdir dist && copy index.html dist\\index.html",
    "start": "serve dist"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
